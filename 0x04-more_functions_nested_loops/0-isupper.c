#include "main.h"


/* more headers goes there */

/**
 * _isupper - checks for uppercase character.
 *
 *@c	:the caracter to test if it's uppercase or not
 * Return:	1 if c is uppercase , 0 otherwise
 */
int _isupper(int c)
{
	return (c >= 65 && c <= 90);
}
