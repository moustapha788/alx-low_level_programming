#include "main.h"


/* more headers goes there */

/**
 * _islower - checks for lowercase character.
 *
 *@c	:the caracter to test if it's lowercase or not
 * Return:	1 if c is lowercase , 0 otherwise
 */
int _islower(int c)
{
	return (c >= 97 && c <= 122);
}
